export interface Props {
  title: string
  date?: string
  location?: string
  tagline: string
  buttonText: string
  videoSrc: string
  link: string
  lang: string
}

const { title, date, location, tagline, buttonText, videoSrc, link, lang } = Astro.props
